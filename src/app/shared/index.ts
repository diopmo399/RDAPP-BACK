/**
 * Barrel export pour tous les modules partagés
 * Permet d'importer facilement depuis n'importe où dans l'application
 *
 * @example
 * import { createSubmitStore, SubmitDataService } from '@shared';
 */
export * from './stores/submit-data';